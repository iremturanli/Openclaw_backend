fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.1,<3.0
python-multipart>=0.0.9
# Used at runtime by the ElevenLabs TTS proxy (and by tests).
httpx>=0.27

# Authentication: password hashing (bcrypt) + JWT access/refresh tokens.
bcrypt>=4.1
pyjwt>=2.8

# Persistence: SQLAlchemy 2.0 async + asyncpg driver; Alembic migrations use the
# psycopg2 (sync) driver.
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29
alembic>=1.13
psycopg2-binary>=2.9

# Tests
pytest>=8.0
pytest-asyncio>=0.23
email-validator>=2.0
openai>=1.40
