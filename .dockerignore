# Keep the build context small and secrets out of the image.
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.git/
.gitignore
tests/
# Secrets are injected at runtime via compose `env_file` — never bake them in.
.env
.env.*
!.env.example
*.md
